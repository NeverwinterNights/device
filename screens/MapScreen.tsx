import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

type MapScreenPropsType = {
    
}

export const MapScreen = ({}:MapScreenPropsType) => {
 return (       
       <View style={styles.container}>
          
        </View>        
    );
};

const styles = StyleSheet.create({
  container: {
  
  }
});
